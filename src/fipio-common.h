#pragma once

#include <stdint.h>
#include <stddef.h>

typedef size_t    fipio_size_t;
typedef ptrdiff_t fipio_diff_t;
typedef uint64_t  fipio_uint_t;
typedef int64_t   fipio_int_t;
typedef uint8_t   fipio_byte_t;
